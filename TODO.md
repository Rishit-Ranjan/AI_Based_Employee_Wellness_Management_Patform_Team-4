# Task: Fix Department Sentiment bars + Add Feedback Logger

## Steps
- [ ] Backend: Update `/api/wellness/sentiments` aggregation to compute sentimentDistribution (positive/neutral/negative %) and include recent feedback logs per department.
- [ ] Frontend: Update `renderDepartmentSentiment` in AdminDashboard.jsx to use the computed sentimentDistribution defensively and display a per-department feedback logger.
- [ ] Verify changes.
