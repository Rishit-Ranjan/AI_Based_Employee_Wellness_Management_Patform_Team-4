# ============================================================================
# PINNED dependency versions for reproducible team environments.
#
# These versions were verified together and are the exact environment in which
# the committed ML artifacts (backend/models/*.pkl) were generated with
# Python 3.13. Using DIFFERENT versions (esp. cloudpickle/pandas/scikit-learn)
# on another machine can make the .pkl models fail to load, which silently
# downgrades personalised recommendations.
# ============================================================================
pandas==3.0.3
numpy==2.5.1
pymongo==4.17.0
bcrypt==5.0.0
PyJWT==2.13.0
python-dotenv==1.2.2
dnspython==2.8.0
scikit-learn==1.9.0
xgboost==3.3.0
flask==3.1.3
flask-cors==6.0.5
requests==2.34.2
joblib==1.5.3
matplotlib==3.11.0
seaborn==0.13.2
flask-jwt-extended==4.7.4
nltk==3.10.0
cloudpickle==3.1.2
reportlab==5.0.0
twilio==9.10.9
waitress==3.0.2